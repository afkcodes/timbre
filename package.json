{
  "name": "timbre",
  "private": true,
  "scripts": {
    "build": "npm --prefix packages/player run build && npm --prefix packages/audio-session run build && npm --prefix packages/media-session run build && npm --prefix packages/cast run build",
    "codegen": "npm --prefix packages/player run codegen",
    "typecheck": "npm --prefix packages/player run typecheck && npm --prefix packages/audio-session run typecheck && npm --prefix packages/media-session run typecheck && npm --prefix packages/cast run typecheck",
    "test": "npm --prefix packages/player run test && npm --prefix apps/example run test",
    "lint": "npm --prefix apps/example run lint",
    "changeset": "changeset",
    "changeset:version": "changeset version && npm install --package-lock-only --ignore-scripts",
    "release": "npm run build && changeset publish",
    "check:readme": "node scripts/check-readme-samples.mjs",
    "check:upstream": "node scripts/check-upstream.mjs",
    "check:links": "node scripts/check-workspace-links.mjs",
    "check:apk": "node scripts/check-workspace-links.mjs --apk apps/example/android/app/build/outputs/apk/debug/app-debug.apk"
  },
  "//typescript": "NOT the version this repo is written in — every package and the example app pin `typescript` themselves (^7.0.2) and their own `tsc` resolves that. This root pin exists for the ESLint stack: typescript-eslint 8 peers `typescript >=4.8.4 <6.1.0`, has no TypeScript 7 support, and npm hoists its packages (`@typescript-eslint/*`, `ts-api-utils`) to the workspace root — where `require('typescript')` resolves HERE. With TypeScript 7 in this slot `ts-api-utils` throws on load (`ts.TypeFlags` is undefined) and `eslint .` cannot run at all. `~6.0.3` is the newest release inside typescript-eslint's peer range. Delete this pin (and the whole workaround) once typescript-eslint supports TypeScript 7.",
  "devDependencies": {
    "@changesets/changelog-github": "^1.0.1",
    "@changesets/cli": "^3.0.3",
    "eslint-plugin-prettier": "^5.5.6",
    "jsdom": "^30.1.0",
    "prettier": "3.9.8",
    "typescript": "~6.0.3"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ]
}
